import { makeStyles } from "@material-ui/core/styles";

const useStyle = makeStyles({
  container: {
    padding: 0,
  },
  appbar: {
    padding: 0,
    background: "#330a7a",
  },
  themeButton: {
    marginLeft: "auto",
  },
});

export default useStyle;
